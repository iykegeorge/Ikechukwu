@model IGUPatients.Models.DispensingUnit

@{
    ViewData["Title"] = "Edit Dispensing Unit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <h2>Edit Dispensing Unit</h2>

<h4>DispensingUnit</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*<input type="hidden" asp-for="DispensingCode" />*@
            <label asp-for="DispensingCode" class="control-label"></label>
            <input asp-for="DispensingCode" class="form-control" />
            <span asp-validation-for="DispensingCode" class="text-danger"></span>

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
